{
  "name": "cocell",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite --host",
    "build": "vite build && npx prettier --write dist/"
  },
  "type": "module",
  "dependencies": {
    "vite": "^6.3.5"
  }
}
